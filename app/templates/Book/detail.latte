{block .title}
	<title>{block title|striptags|firstupper}{$nazev} - Knihovna{/block}</title>
{/block}

{block content}
	<h2>{$nazev}</h2>
		<div class="clr"></div>
		<div n:foreach="$flashes as $flash" class="flash {$flash->type}">{$flash->message}</div>
		<h3 class="titlePage">Upravit knihu</h3>

{/block}